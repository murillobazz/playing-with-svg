# Testing some SVG animations

## - firewatch-view is a view entirely made with svg that changes between day (light) mode and night (dark) mode without any hooks, global states, etc. Just pure and raw js and css.